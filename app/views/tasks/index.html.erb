<h1>tasklist</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %> : <%= task.status %></li>
  <% end %>
</ul>

<%= link_to 'create new task', new_task_path %>